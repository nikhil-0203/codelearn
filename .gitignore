# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Environment files (NEVER push these)
server/.env
client/.env
client/.env.production
client/.env.local

# Build output
client/build/

# Uploads (stored on Cloudinary in production)
server/uploads/